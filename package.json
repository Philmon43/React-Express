{
  "name": "react-express",
  "version": "1.0.0",
  "description": "react-express project",
  "main": "index.js",
  "scripts": {
    "heroku-postbuild": "cd clinet && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
    "start": "node server/app.js"
  },
  "engines": {
    "node": "12.18.1"
  },
  "author": "philmon",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
